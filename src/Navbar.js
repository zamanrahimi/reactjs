const Navbar = () => {

    return (
        <div className="navbar">

            <div className="links">
                <a href="#">Home</a>
                <a href="#">Contact</a>
                <a href="#">Gollery</a>
            </div>

        </div>

        );
}

export default Navbar;